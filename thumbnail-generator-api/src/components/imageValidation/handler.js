const {createImage,} = require("./application/createImage/createImage.application");
const { getImage } = require("./application/getImage/getImage.application");

module.exports = {
  createImage,
  getImage,
};
